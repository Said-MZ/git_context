{
  "name": "git-context",
  "version": "1.0.0",
  "description": "Analyzes Git branches and outputs structured, LLM-ready context with intelligent risk analysis",
  "main": "dist/index.js",
  "bin": {
    "git-context": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["git", "cli", "context", "llm", "diff", "analysis"],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0"
  }
}
